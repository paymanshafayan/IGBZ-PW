# فاز ۷ — لجستیک و ارسال: طراحی اصلاح‌شده (اپ پیک IGBZ + برچسب + COD)

**وضعیت:** نسخهٔ دوم — بازنویسی پس از بازبینی کارفرما. نسخهٔ اول (دسته‌بندی بر اساس
شهر/وزن) **ناقص بود** و مزیت رقابتی نداشت؛ این نسخه تعریف کامل کارفرما را پیاده می‌کند.

> **اصل حاکم:** فاز ۷ فقط زمانی معنا دارد که «اپلیکیشن پیک IGBZ» وجود داشته باشد. بدون آن،
> همهٔ این کارها با افزونه‌های موجود انجام‌شدنی است و مزیتی برای محصول نیست.

---

## ۱. تصویر کامل (از سفارش تا تحویل)

```
سفارش پرداخت‌شده
   ↓
۱) گروه‌بندی مرسوله‌ها (بر اساس مسیر/سرویس/بازه) ← اپراتور گروه را انتخاب می‌کند
   ↓
۲) چاپ برچسب‌های استاندارد (پست/تیپاکس) — هر برچسب بارکد یکتا دارد
   ↓
۳) پیک بسته‌ها را می‌گیرد → اپ پیک IGBZ:
      · اسکن بارکد → صفحهٔ همان محصول باز می‌شود
      · بسته‌های روز بر اساس لوکیشن شهری مرتب و مسیریابی می‌شوند
   ↓
۴) تحویل:
      · پیک بارکد را اسکن می‌کند → صفحهٔ محصول در اپ پیک
      · پیک کد (PIN) دریافتی از مشتری را وارد می‌کند
      · اگر COD باشد:
          ├─ مشتری اپ مشتری دارد → با اپ خودش بارکد را اسکن و پرداخت می‌کند
          │    → وضعیت «پرداخت در محل» در اپ پیک → «پرداخت‌شده» تغییر می‌کند
          └─ مشتری اپ ندارد → سه گزینه در اپ پیک:
               ۱) پرداخت نقدی (پیک وجه را می‌گیرد و انتخاب می‌کند)
               ۲) درگاه: لینک پرداخت برای مشتری پیامک می‌شود → نتیجه در صفحهٔ پیک
               ۳) کارت‌به‌کارت: شماره‌کارت(ها) پیامک می‌شود + باکس «ثبت کد رهگیری»
                  (مشتری پس از پرداخت، کد رهگیری را به پیک می‌دهد و پیک ثبت می‌کند)
      · پس از هر مسیر: محصول → «تحویل شد»
```

---

## ۲. موجودیت‌ها و نقش‌ها

### نقش‌ها
| نقش | دسترسی |
|---|---|
| **اپراتور فروشگاه** | گروه‌بندی و چاپ برچسب، مدیریت مرسوله‌ها |
| **پیک (Courier)** | فقط بسته‌های خودش: دیدن، اسکن، مسیریابی، تأیید تحویل/COD — **PIN و مبالغ را نمی‌بیند** |
| **مشتری** | اپ مشتری: اسکن بارکد در لحظهٔ تحویل + پرداخت COD؛ **فقط مشتری PIN را دارد** |

### جدول‌ها (تکمیل v15)
| جدول | اضافه‌ها |
|---|---|
| `ig_shipments` | + `courier_id`، `barcode` (یکتا)، `label_group_id`، `status` (…، `out_for_delivery`، `awaiting_cod`، `delivered`) |
| `ig_couriers` | پیک‌ها: `user_id`، `name`، `phone`، `vehicle`، `zone`، `is_active` |
| `ig_label_groups` | گروه‌های چاپ: `tenant_id`، `title`، `status`، `created_by`، `created_at` |
| `ig_label_group_items` | مرسوله‌های هر گروه (many-to-many) |
| `ig_cod_payments` | تلاش‌های COD: `shipment_id`، `method` (`app`/`cash`/`gateway`/`card`)، `status`، `amount`، `ref`، `gateway_link`، `card_transfer_ref`، `timestamps` |
| `ig_courier_routes` | مسیر محاسبه‌شدهٔ پیک: `courier_id`، `shipment_ids` (ترتیب)، `payload`، `created_at` |
| `ig_courier_tracking` | موقعیت زنده: `shipment_id`، `lat`/`lng`، `at` (هر N ثانیه از اپ پیک) |
| `ig_courier_chat` | چت پیک↔مشتری: `shipment_id`، `sender` (`courier`/`customer`)، `body`، `read_at`، `created_at` |

### بارکد
- مقدار: `IGBZ-<shipment_id>-<random>` (یکتا، قابل اسکن)
- فرمت: **Code 128** (یا QR برای اپ مشتری) — تولید سمت سرور، بدون وابستگی خارجی.

---

## ۳. برچسب استاندارد (Label Printing)

### گروه‌بندی
- صفحهٔ «Label printing» در پنل: فیلتر بر اساس مسیر (`express`/`national`/`heavy`)، سرویس
  (پست/تیپاکس/پیک)، بازهٔ سفارش، وضعیت.
- «ساخت گروه» → جدول `ig_label_groups` + اقلام. اپراتور هر گروه را انتخاب و چاپ می‌کند.

### قالب چاپ
- **A4، چند برچسب در هر صفحه** (مثلاً ۲×۴) با CSS `@media print`.
- هر برچسب شامل: آدرس مبدأ/مقصد، گیرنده، وزن، بارکد، **کد رهگیری (خالی تا ثبت در هاب)**،
  «تحویل در محل» در صورت COD، و **بخش PIN برای مشتری** (خطچین جدا).
- **استاندارد پست/تیپاکس:** ابعاد برچسب و فیلدها مطابق الزام آن سرویس‌ها (قالب قابل تنظیم
  در تنظیمات؛ چون هر هاب قالب خودش را دارد).

### جریان چاپ
`ساخت گروه ← انتخاب گروه ← چاپ (مرورگر/چاپگر حرارتی) ← برچسب‌ها با بارکد`

---

## ۴. اپلیکیشن پیک IGBZ (REST + اپ)

### احراز هویت پیک
- نقش `igbz_courier` (capability جدا). پیک با شماره موبایل/OTP یا JWT وارد می‌شود.
- دسترسی محدود: فقط `ig_shipments` با `courier_id = خودش`.

### مسیریابی لوکیشنی (هسته)
- هر مرسوله مختصات (`lat`/`lng`) دارد (از آدرس/گیرنده).
- «مسیر امروز»: الگوریتم **نزدیک‌ترین همسایه (Nearest Neighbour)** روی نقاط → ترتیب
  بازدید بهینه → `ig_courier_routes`.
- بدون وابستگی به نقشهٔ خارجی؛ مسیر به‌صورت «نقاط متوالی» در اپ نمایش داده می‌شود
  (در آینده با نقشهٔ داخلی/مسیریابی واقعی قابل ارتقاست).
- پیک می‌تواند ترتیب را دستی تغییر دهد.

### اسکن بارکد
- دوربین اپ → خواندن بارکد → `GET /igbz/v1/courier/shipments/{barcode}` → صفحهٔ همان
  مرسوله (با دسترسی پیک).

### تأیید تحویل (غیر-COD)
- پیک PIN دریافتی از مشتری را وارد می‌کند → `POST /courier/shipments/{id}/deliver {pin}`
  → بررسی با `hash_equals` → «تحویل شد».

### جریان COD (هستهٔ جدید)
```
پیک بارکد را اسکن کرد → صفحهٔ محصول (وضعیت: پرداخت در محل)
  → پیک PIN را وارد کرد
  ├─ اپ مشتری (تشخیص: آیا مشتری اپ دارد؟ — از وضعیت دستگاه/کد)
  │    مشتری با اپ خودش بارکد را اسکن می‌کند
  │    → POST /igbz/v1/checkout/cod-pay { shipment_barcode, ... }
  │    → درگاه (کیف پول/درگاه) → پرداخت
  │    → وضعیت در اپ پیک به‌صورت زنده «پرداخت‌شده» می‌شود (poll یا push)
  └─ بدون اپ → سه گزینه در اپ پیک:
       ۱) نقدی: POST /courier/cod { method: cash } → تأیید دریافت وجه
       ۲) درگاه: POST /courier/cod { method: gateway } → لینک پرداخت ساخته و
          برای مشتری پیامک می‌شود → مشتری پرداخت می‌کند → نتیجه به اپ پیک برمی‌گردد
          (poll روی وضعیت پرداخت / وبوک)
       ۳) کارت‌به‌کارت: POST /courier/cod { method: card } → شماره‌کارت(ها) پیامک
          می‌شود + باکس «ثبت کد رهگیری» در اپ پیک → مشتری پس از پرداخت کد را می‌دهد
          → پیک ثبت می‌کند → (اختیاری: تطبیق دستی بعدی)
  → پس از هر مسیر: ig_cod_payments ثبت و مرسوله → «تحویل شد»
```

### هماهنگی «اپ مشتری → اپ پیک» (زنده)
- پرداخت COD در اپ مشتری، `ig_cod_payments` را با `status=paid` ثبت می‌کند؛
- اپ پیک با **poll سبک** (هر N ثانیه روی همان shipment) یا **push (FCM)** وضعیت را
  می‌گیرد و «پرداخت در محل → پرداخت‌شده» را نشان می‌دهد.
- ضد پرداخت دوباره: فقط یک `ig_cod_payments` موفق به‌ازای هر shipment.

---

## ۵. اندپوینت‌های REST (افزوده بر فاز ۷ قبلی)

### پیک (`igbz/v1/courier/*` — auth پیک)
```
GET    /courier/me
GET    /courier/shipments                      (بسته‌های امروز من)
GET    /courier/shipments/{barcode}            (اسکن → صفحهٔ محصول)
POST   /courier/routes/plan                    (مسیریابی لوکیشنی → ig_courier_routes)
GET    /courier/routes/today
POST   /courier/shipments/{id}/deliver         { pin }
POST   /courier/shipments/{id}/cod             { method: cash|gateway|card }
POST   /courier/shipments/{id}/cod/confirm     { method: card, card_transfer_ref }
GET    /courier/shipments/{id}/cod/status      (نتیجهٔ درگاه/اپ)
POST   /courier/tracking/{shipment_id}         { lat, lng } — موقعیت زنده (هر N ثانیه)
GET    /courier/chat/{shipment_id}             (پیام‌های چت)
POST   /courier/chat/{shipment_id}/send        { body } — چت با مشتری
```

### مشتری (COD در لحظهٔ تحویل + رصد)
```
POST   /igbz/v1/checkout/cod-pay               { shipment_barcode } → پرداخت
GET    /igbz/v1/checkout/cod/status            { shipment_barcode }
GET    /igbz/v1/shipments/{id}/status          (وضعیت مرسوله در اپ مشتری)
GET    /igbz/v1/shipments/{id}/tracking        (موقعیت زنده روی نقشه — پس از «تحویل به پیک»)
GET    /igbz/v1/shipments/{id}/chat            (چت با پیک)
POST   /igbz/v1/shipments/{id}/chat/send       { body }
```

### اپراتور (پنل)
```
GET    /igbz/v1/admin/label-groups             (فهرست گروه‌ها)
POST   /igbz/v1/admin/label-groups             (ساخت گروه از فیلتر)
POST   /igbz/v1/admin/label-groups/{id}/print  (تولید HTML چاپی)
GET    /igbz/v1/admin/label-groups/{id}/barcodes
GET/POST /igbz/v1/admin/couriers               (مدیریت پیک‌ها)
```

---

## ۵ب. رصد زنده و ارتباط (الهام از اپ رانندگان اسنپ‌باکس)

- **رصد زنده در اپ مشتری:** پس از تغییر وضعیت به «تحویل به پیک»، مشتری موقعیت زندهٔ
  مرسوله را روی نقشه می‌بیند. فنی: اپ پیک هر N ثانیه GPS می‌فرستد
  (`POST /courier/tracking/{id}` → `ig_courier_tracking`) و اپ مشتری با poll سبک یا FCM
  موقعیت را می‌گیرد. (برای مقیاس بزرگ‌تر: MQTT/WebSocket — الگوی اسنپ که از MQTT + Pusher
  fallback استفاده می‌کند.)
- **تماس با مشتری در اپ پیک:** دکمهٔ تماس (درون‌برنامه‌ای صوتی یا تلفنی).
- **چت پیک ↔ مشتری:** متن آزاد + پیام‌های ازپیش‌تعریف‌شده (مثل اسنپ برای حفظ تمرکز راننده)،
  در `ig_courier_chat`؛ خوانده/نخوانده با `read_at`.

---

## ۶. امنیت و قواعد
- **PIN فقط مشتری:** هرگز در اپ پیک/پاسخ REST پیک نمایش داده نمی‌شود؛ فقط `hash` مقایسه می‌شود.
- **بارکد یکتا** (`UNIQUE`)؛ اسکن با بارکدِ ناموجود → 404.
- **COD ضد دوباره‌پرداخت:** یک رکورد موفق به‌ازای هر مرسوله (UNIQUE).
- **دسترسی پیک:** فقط مرسوله‌های خودش (شرط `courier_id` در همهٔ کوئری‌ها).
- **مبالغ:** پیک مبلغ را نمی‌بیند مگر در لحظهٔ COD که لازم است (فقط «پرداخت در محل» بودن).
- **مسیریابی:** محاسبهٔ سمت سرور؛ اپ فقط نمایش می‌دهد (قابل‌ارتقا به نقشهٔ داخلی بعداً).

---

## ۷. فازبندی پیاده‌سازی (پس از تأیید)
| گام | محتوا |
|---|---|
| ۱ | جدول‌ها (couriers, label_groups, cod_payments, routes) + مهاجرت v16 |
| ۲ | نقش پیک + REST پیک (بسته‌ها/اسکن/تحویل/COD) |
| ۳ | مسیریابی لوکیشنی (Nearest Neighbour) + ig_courier_routes |
| ۴ | برچسب‌ها: گروه‌بندی + قالب A4 + بارکد + صفحهٔ چاپ |
| ۵ | COD کامل: اپ مشتری (اسکن + پرداخت) + سه‌گانه (نقدی/درگاه/کارت‌به‌کارت) + پیامک |
| ۶ | تست‌ها + تأیید زنده |
