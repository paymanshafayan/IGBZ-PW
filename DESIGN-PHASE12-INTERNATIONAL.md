# فاز ۱۲ — بین‌المللی‌سازی: چندزبانه، درگاه ارزی و ترجمهٔ خودکار: طراحی

**وضعیت:** طراحی تأییدشده توسط کارفرما. مبنای آن: نقشهٔ راه nop (فاز ۱۲) + تعاریف تکمیلی
کارفرما (چندزبانه با انتخاب ادمین، درگاه ووکامرس در کنار NOWPayments، تسویه به قبرس).

> **جایگاه:** «درآمد ارزی فروشگاه از مشتری خارجی» — جدا از ماژول FX (هزینهٔ ابزارها) و جدا
> از فاز ۶ (داخلی/ریالی). طبق فاز ۶، ارزی فقط از «درگاه ارزی مرکزی» با کارمزد جزئی.

---

## ۱. فروشگاه چندزبانه (با انتخاب ادمین)

### ۱.۱ قابلیت
- ادمین زبان(های) فروشگاه را **انتخاب** می‌کند: فارسی (پیش‌فرض) + یک یا چند زبان
  (انگلیسی، عربی، ترکی، کردی و...).
- **ترجمهٔ خودکار** محتوا: نام/توضیحات محصول + صفحات کلیدی (قوانین، درباره، راهنما) با
  سرویس ترجمه (ترجمیار/فرازین/دیپ‌فا — `HttpTranslationAdapter` موجود).
- **انتخابگر زبان** در سایت/اپ برای مشتری.
- ذخیره: متای ترجمه‌شده روی محصول (`igbz_translation_<lang>` — موجود) + صفحات ترجمه‌شده.

### ۱.۲ جریان
```
ادمین → تنظیمات → زبان‌ها: انتخاب زبان‌ها ← فعال
  → دکمهٔ «ترجمهٔ همهٔ محصولات» (یا خودکار هنگام ثبت محصول)
  → ترجمه با سرویس → ذخیره در متا → نمایش به مشتری با انتخابگر زبان
```

---

## ۲. درگاه‌های ارزی (درگاه ارزی مرکزی)

| درگاه | روش | وضعیت | نکته |
|---|---|---|---|
| **NOWPayments** | کریپتو (USDT/BTC) + **کارت بین‌المللی** (تسویه USDT) | ✅ موجود (`NowPaymentsGateway`) | بدون نیاز به حساب بانکی؛ کارمزد جزئی درگاه مرکزی |
| **درگاه ووکامرس (Stripe/PayPal)** | کارت بانکی (Visa/MC) — با حساب بانکی قبرس | 🟡 جدید (فعال‌سازی) | **وقتی کارت بانکی قبرس فعال شد**، این درگاه در دسترس است |

### ۲.۱ فعال‌سازی Stripe/PayPal
- ووکامرس درگاه‌های رسمی Stripe و PayPal دارد → فعال‌سازی با **کلیدهای حساب قبرسی** (شرکت).
- در checkout، مشتری خارجی می‌تواند: کریپتو (NOWPayments) یا کارت (Stripe/PayPal).
- تسویه: به حساب شرکت در قبرس.

### ۲.۲ ترتیب پیشنهادی
1. NOWPayments (بدون نیاز به حساب بانکی — از ابتدا فعال)
2. Stripe/PayPal (وقتی حساب بانکی قبرس آماده شد)

---

## ۳. جدول‌ها/تنظیمات
| مورد | محتوا |
|---|---|
| `i18n.enabled` / `i18n.languages` | چندزبانه: فعال/غیرفعال + فهرست زبان‌ها (انتخاب ادمین) |
| `i18n.default_language` | زبان پیش‌فرض (فارسی) |
| `nowpayments.*` | موجود (API key، pay_currency، usd_rate_irt) |
| `stripe.enabled` / `stripe.secret_key` / `paypal.enabled` / `paypal.client_id` | درگاه‌های ووکامرس (کلیدهای حساب قبرس) |
| `ig_product_translations` (یا متای موجود) | ترجمه‌های محصول به‌ازای زبان |

---

## ۴. اندپوینت‌ها
```
GET  /igbz/v1/app/config            (موجود) → + زبان‌های فعال
GET  /igbz/v1/catalog/products      (موجود) → + پارامتر lang
POST /igbz/v1/admin/i18n/languages  (انتخاب زبان‌ها)
POST /igbz/v1/admin/i18n/translate-all  (ترجمهٔ همهٔ محصولات)
```

---

## ۵. قواعد
- چندزبانه **با انتخاب ادمین** (پیش‌فرض فقط فارسی — غیرفعال تا ادمین فعال کند).
- ارزی فقط از درگاه ارزی مرکزی (NOWPayments + Stripe/PayPal) با کارمزد جزئی.
- ترجمه از سرویس واقعی (نه رشتهٔ ثابت — الگوی پروژه).

---

## ۶. فازبندی پیاده‌سازی (پس از تأیید)
| گام | محتوا |
|---|---|
| ۱ | چندزبانه: تنظیمات + انتخابگر زبان + ترجمهٔ محصول/صفحات |
| ۲ | Stripe/PayPal: فعال‌سازی با کلید قبرس + نمایش در checkout |
| ۳ | اتصال NOWPayments به درگاه مرکزی (کارمزد جزئی) |
| ۴ | ترجمهٔ خودکار سراسری + کرون |
| ۵ | تست‌ها + تأیید زنده |
