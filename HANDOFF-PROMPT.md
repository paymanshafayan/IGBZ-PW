# پرامپت شروع چت جدید

متن زیر را عیناً در چت تازه کپی کنید.

---

سلام. این پروژه از قبل شروع شده، کد زیادی دارد و چند فاز تکمیل شده است. کار این چت **ادغام**
است، نه ساختن چیز تازه. همهٔ پاسخ‌هایت را **فارسی** بنویس و **مرحله‌به‌مرحله** جلو برو.

## در یک نگاه

مخزن `paymanshafayan/IGBZ-WP`. دو چیز داخلش هست:

1. **محصول IGBZ** — پورت از nopCommerce به **وردپرس + ووکامرس**، یک افزونه با پنج ماژول
   قابل‌خاموش‌وروشن (`multitenant`, `instagram`, `hub`, `rest_api`, `fx`). دیتابیس v17 با
   **۷۰ جدول**. فازهای ۶ تا ۱۴ و واسط پرداخت ارزی تمام شده‌اند.
2. **هوشا** (`hoosha/`) — ابزار عاملِ خودمان، نسخهٔ **۰.۹.۲**: هستهٔ عامل، هاب پرووایدر با
   عیب‌یاب، رابط دوزبانه، اتصال گیت با مخزن قفل‌شوندهٔ هر گفتگو، سندباکس کانتینری.
   ۳۶۵ تست واحد + ۴۴ آزمون زنده.

کار محصول تا PR شمارهٔ ۲ روی `main` ادغام شده (`43d89d9` و `c3f700a`). کارهای بعدی —
هماهنگ‌سازی اسناد، `.pot` و ابزار `makepot`، و **کل هوشا از ۰.۷ تا ۰.۹.۲** — روی شاخهٔ
جلسهٔ قبلی `arena/01a00c31-igbz-wp` است و هنوز ادغام نشده.

> شاخهٔ کاری این جلسه را **از سیستم بگیر** (`git status`)، نه از متن این سند. هر جلسه شاخهٔ
> `arena/…` خودش را دارد. `origin` منبع حقیقت است؛ اگر گیت محلی به‌هم ریخت:
> `git fetch origin && git reset --hard origin/<branch>`.

## مرحلهٔ ۱ — اسناد را بخوان

1. `PROJECT-STATE.md` — **اول این.** وضعیت کامل، تصمیم‌های ثبت‌شدهٔ کارفرما، و بخش **۷.۵**
   که هوشا را توضیح می‌دهد.
2. `MODULES-REFERENCE.md` — مرجع ماژول‌ها و زیرسیستم‌ها.
3. `igbz-suite/README.md` — مرجع فنی افزونه · `_devenv/AGENT-BRIEF.md` — تله‌های شناخته‌شده.
4. `hoosha/README.md` — مرجع کامل هوشا · `hoosha/DESIGN-UI-PARITY.md` — تاریخچهٔ رابط و
   هر باگی که پیدا شد · `hoosha/DESIGN-PROVIDER-HUB.md` — هاب و عیب‌یاب.
5. سندهای طراحی فازها: `DESIGN-PHASE6-PAYMENTS.md`، `DESIGN-PHASE7-COURIER.md`،
   `DESIGN-PHASE9-MARKETPLACES.md`، `DESIGN-PHASE10-SEO.md`،
   `DESIGN-PHASE12-INTERNATIONAL.md`، `DESIGN-DOMAIN.md`، `DESIGN-LEGAL-AUTH.md`،
   `DESIGN-FX.md`، `DESIGN-APPS-ROADMAP.md`، `DESIGN-DEPLOY-HOOSHA.md`، `API-KEYS.md`.

## مرحلهٔ ۲ — هر دو را بالا بیاور و بسنج

**افزونه** (محیط توسعهٔ آفلاین داخل خود مخزن است):

```bash
ls _devenv/.work/            # اگر خالی/نبود:
bash _devenv/setup.sh        # ~۲۳ ثانیه
bash _devenv/run.sh          # http://127.0.0.1:9400 با ورود خودکار مدیر
bash _devenv/test.sh         # باید بدهد: ۱۲۰۹ اظهارنظر / ۲۳ کیس، ۲۳۵ فایل بدون خطا
```

**هوشا:**

```bash
cd hoosha && npm ci
node test/run.mjs            # ۳۶۵ تست واحد (~۳۰ ثانیه)
node test/live.mjs           # ۴۴ آزمون زنده روی سرور واقعی
node src/cli.js --version    # باید نسخه + شناسهٔ کامیت + مسیر کد را بدهد
```

نکته‌هایی که وقتت را می‌گیرند اگر ندانی:

- بالا آمدن سایت ۳۰ تا ۹۰ ثانیه طول می‌کشد؛ یک ۵۰۲ قبل از اولین ۲۰۰ طبیعی است.
- روی نصب تازه، ووکامرس پنل را با ویزارد می‌دزدد؛ یک بار این را باز کن:
  `admin.php?page=wc-admin&path=%2Fsetup-wizard&step=skip-guided-setup`
- `_devenv/.work/` و `hoosha/node_modules/` بین جلسه‌ها پاک می‌شوند. سلامت پایگاه:
  `/?igbz_health=1` باید ۷۰/۷۰ بدهد.
- هوشا را همیشه از ریشهٔ مخزن با `.\hoosha.cmd` / `./hoosha.sh` اجرا کن؛
  `node src/cli.js` از ریشه یعنی `MODULE_NOT_FOUND` با `requireStack: []`.
- **سندباکس هوشا پیش‌فرض روشن است.** روی ماشینی بدون داکر، `bash` عمداً اجرا نمی‌شود.
  برای کار روی پروژهٔ واقعی، تنظیمات ← سندباکس را صریح خاموش کن.

## مرحلهٔ ۳ — کار این چت: ادغام

هدف، بستن **PR شمارهٔ ۳** از شاخهٔ جلسهٔ قبلی به `main` است. پیش از هر کاری:

1. `git log --oneline main..arena/01a00c31-igbz-wp` را ببین و **خلاصهٔ خوانا** از آنچه
   ادغام می‌شود بنویس (چند دستهٔ معنایی، نه فهرست کامیت).
2. `git diff --stat main...arena/01a00c31-igbz-wp` — حجم و دامنهٔ تغییر.
3. مطمئن شو هر دو سوئیت روی همان شاخه سبزند (بالا).
4. تعارض احتمالی با `main` را زودتر پیدا کن: `git merge-base`، و یک ادغام آزمایشی محلی
   (`git merge --no-commit --no-ff origin/main`) که بعدش `git merge --abort` می‌کنی.
5. **گزارش بده و بایست.** بدون تأیید صریح کارفرما، PR نساز و چیزی را ادغام نکن.

اگر تأیید گرفتی، PR را با `gh pr create` از همان شاخه بساز؛ عنوان و متن فارسی، با سه بخش:
چه چیزی عوض شد، چرا، و چه چیزی هنوز باز است.

## قاعده‌های ثابت

- **فارسی** بنویس.
- **بدون تغییر بدون پلن تأییدشده.** خواندن و بررسی و گزارش آزاد است؛ هر تغییر کد/سند/کامیت
  فقط بعد از پلن مکتوب و تأیید صریح.
- **تأیید زنده، نه فقط تست واحد.** بعد از هر تغییر معنادار روی سایت (یا رابط هوشا) کلیک کن.
  چند باگ فقط این‌طور پیدا شدند.
- **سوئیتی که بار اول سبز شود چیزی ثابت نکرده.** تست تازه را با خراب‌کردن عمدی کد قرمز کن.
- **تست‌ها را ته کار بگیر، نه بعد از هر ویرایش** (خواستهٔ کارفرما). حین کار:
  `node test/run.mjs --only <الگو>`؛ پیش از کامیت، اجرای کامل.
- **قانون Graph API (تصحیح‌شده):** استفاده نمی‌کنیم چون در ایران به‌عنوان سرویس‌دهنده در
  دسترس نیست؛ اگر سرویسی مهیایش کند، استقبال می‌کنیم. اتوماسیون مرورگر (مثل Windsor) طبق
  ToS متا ممنوع است — هرگز پیشنهاد نده.
- هر پرامپتی که تمام می‌شود باید **گزارش مکتوب در اسناد** بگذارد؛ مستندسازی بخشی از
  تمام‌کردن کار است.
- فقط روی شاخهٔ جلسه کار کن — از سیستم بگیرش، نه از متن اسناد.
- قواعد فنی افزونه (جزئیات در `PROJECT-STATE.md` §۶): SQL همیشه MySQL؛ هرگز `__()` قبل از
  `init`؛ شمارش جدول‌ها با `grep -c '$sql[] = "CREATE TABLE'` (اکنون ۷۰)؛ تلهٔ
  `wpdb::$field_types`؛ `wp_get_referer()` در self-POST؛ `random_int`؛ لجر به‌جای موجودیِ
  ذخیره‌شده؛ idempotency مالی؛ `Logger::info` با پارامتر دوم **رشته**؛ آداپتور درگاه به
  `AbstractIpgGateway` و `use IGBZ\Suite\Support\Http;` نیاز دارد.

## آنچه هنوز باز است

1. **طراحی هستهٔ هوشواره (AI مرکزی)** — فقط با دستور جداگانهٔ کارفرما.
2. **اپ‌های فلاتر** (ادمین/مشتری/پیک + PWA) — در ریپوی اپ‌ها، طبق `DESIGN-APPS-ROADMAP.md`.
3. **کلیدهای واقعی** سرویس‌ها — آداپتورها آماده‌اند.
4. **ترجمهٔ `fa_IR.po`** — قالب `.pot` به‌روز است (۲۳۰۸ رشته)؛ خودِ ترجمه تصمیم کارفراست.
5. **استقرار هوشا روی سرور** — `DESIGN-DEPLOY-HOOSHA.md` نوشته شده، پیاده نشده، هفت سؤال باز.
6. در خود هوشا: عدد بودجهٔ ماهانه · Fusion (تصمیم ۹) · elicitation در MCP · ترمینال داخلی.
