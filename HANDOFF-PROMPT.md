# پرامپت شروع چت جدید

متن زیر را عیناً در چت تازه کپی کنید.

---

سلام. این پروژه از قبل شروع شده، کد زیادی دارد و چندین فاز تکمیل شده است. قبل از هر کاری این
سه مرحله را به‌ترتیب انجام بده و **مرحله به مرحله جلو برو**. همهٔ پاسخ‌هایت را **فارسی** بنویس.

## مرحلهٔ ۱ — اسناد را بخوان

مخزن `paymanshafayan/IGBZ-WP` است. شاخهٔ کاری را **از سیستم بگیر** — هر جلسه یک شاخهٔ
`arena/…` اختصاصی دارد و نباید شاخه‌ای را از متن این سند hardcode کرد. کار اصلی محصول روی
`main` ادغام شده (PR شمارهٔ ۱، کامیت `43d89d9`)؛ کارهای جاری روی شاخهٔ جلسه‌اند و در PR
شمارهٔ ۲ جمع می‌شوند. `origin` منبع حقیقت است؛ اگر گیت محلی به هم ریخت:
`git fetch origin <branch> && git reset --hard FETCH_HEAD`.

این اسناد را به همین ترتیب بخوان:

1. `PROJECT-STATE.md` — **اول این.** وضعیت کامل پروژه: چه ساخته شده، در کدام کامیت، چرا، و
   چه تصمیم‌هایی هنوز باز است.
2. `MODULES-REFERENCE.md` — مرجع ماژول‌ها: شرح کارکرد و مستندات فنی همهٔ ماژول‌ها و
   زیرسیستم‌ها (سرویس‌ها، جداول، کرون‌ها، صفحات ادمین، REST، کلیدهای تنظیمات).
3. `igbz-suite/README.md` — مرجع فنی افزونه: نصب، تنظیمات، صفحات مدیر، REST API، معماری.
4. `_devenv/AGENT-BRIEF.md` — جزئیات درونی زیرسیستم‌ها و تله‌هایی که قبلاً وقت زیادی گرفته‌اند.
5. سندهای طراحی فازهای اخیر: `DESIGN-PHASE6-PAYMENTS.md`، `DESIGN-PHASE7-COURIER.md`،
   `DESIGN-PHASE9-MARKETPLACES.md`، `DESIGN-PHASE10-SEO.md`، `DESIGN-PHASE12-INTERNATIONAL.md`،
   `DESIGN-DOMAIN.md`، `DESIGN-LEGAL-AUTH.md`، `DESIGN-FX.md`.
6. `DESIGN-APPS-ROADMAP.md`، `API-KEYS.md`، `PROMPTS-SEO-HUSHVARE.md`،
   `PROMPT-IG-GROWTH-HUSHVARE.md`.
7. `_devenv/README.md` — محیط توسعهٔ آفلاین.

خلاصهٔ خیلی کوتاه: پورت محصول IGBZ از nopCommerce به **وردپرس + ووکامرس** به شکل **یک افزونه
با پنج ماژول قابل‌خاموش‌وروشن** (`multitenant`, `instagram`, `hub`, `rest_api`, `fx`).
دیتابیس در نسخهٔ ۱۶ با **۶۹ جدول** است. فازهای ۶ تا ۱۴ نقشهٔ راه nop (درگاه‌های بانکی،
درگاه مرکزی escrow، BNPL، اپ پیک، مارکت‌پلیس‌ها، سئو، ترجمه، دامنه، گیمیفیکیشن، قرعه‌کشی،
هوشوارهٔ موقت) و واسط پرداخت ارزی (فازهای ۱–۳b) **تمام شده‌اند**. تست‌ها: ۱۱۷۲ اظهارنظر در
۲۳ کیس، ۲۳۴ فایل لینت صفر خطا.

**قانون اینستاگرام (تصحیح‌شده — مهم):** عدم استفاده از Graph API **تنها به‌دلیل عدم امکان
دریافت آن به‌عنوان سرویس‌دهنده در ایران است** — نه «نخواستن»، بلکه «نتوانستن». **اگر سرویسی
بتواند Graph API را برای ما مهیا کند، از آن استقبال می‌کنیم.** این قید موقت است، نه ممنوعیت.
پروژه فعلاً با Manus و ManyChat (غیرفعال، به‌عنوان fallback) و **ChatPlace** (کلاینت آماده،
سوئیچ `dm.provider = manychat|chatplace`) اجرا می‌شود.

## مرحلهٔ ۲ — محیط را بالا بیاور

محیط توسعهٔ آفلاین داخل خود مخزن است، چون سندباکس به wordpress.org دسترسی ندارد:

```bash
ls _devenv/.work/            # اگر خالی/نبود:
bash _devenv/setup.sh        # ~۲۳ ثانیه
bash _devenv/run.sh          # http://127.0.0.1:9400 با ورود خودکار مدیر
bash _devenv/test.sh         # باید بدهد: ۱۱۷۲ اظهارنظر / ۲۳ کیس، ۲۳۴ فایل بدون خطا
```

سه نکته که وقتت را می‌گیرند اگر ندانی:
- بالا آمدن سایت ۳۰ تا ۹۰ ثانیه طول می‌کشد و یک خطای ۵۰۲ قبل از اولین ۲۰۰ طبیعی است.
- روی نصب تازه، ووکامرس پنل مدیر را با ویزارد راه‌اندازی می‌دزدد. یک بار این آدرس را باز کن:
  `admin.php?page=wc-admin&path=%2Fsetup-wizard&step=skip-guided-setup`
- `_devenv/.work/` بین جلسه‌ها پاک می‌شود؛ ولی چیزی از کد پروژه آنجا نیست. لاگ خطا:
  جدیدترین `/tmp/node-playground-cli-site-*/wordpress/wp-content/debug.log`.
- سلامتی پایگاه: `/?igbz_health=1` باید ۶۹/۶۹ جدول بدهد.

بعد وارد پنل شو، منوی **IGBZ** را باز کن و صفحات را ببین تا با محصول آشنا شوی — به‌خصوص
**Payments** (با دکمه‌های Test درگاه‌ها)، **Master payment**، **Logistics**، **Domain**،
**VIP channel**، **Courses** و **FX payments**.

## مرحلهٔ ۳ — گزارش بده و بایست

وقتی اسناد را خواندی و محیط بالا آمد، به من گزارش بده:

- محیط بالا آمد یا نه، و تست‌ها چه عددی دادند.
- از خواندن اسناد چه فهمیدی — وضعیت پروژه را با زبان خودت خلاصه کن.
- اگر جایی از اسناد با کد نخواند، همان‌جا را بگو.
- کدام تصمیم‌ها منتظر رأی من است.

**بعد از گزارش، متوقف شو و منتظر بمان.** خودت کاری را شروع نکن، هیچ فایلی از پروژه را عوض
نکن، و پیشنهاد اجرا نده مگر بخواهم. من بعد از گزارش می‌گویم چه کاری انجام شود.

## چند قاعدهٔ ثابت

- **فارسی** بنویس.
- **بدون تغییر بدون پلن تأییدشده:** هیچ تغییری در پروژه (کد، اسناد، کامیت، پوش) مگر بعد از
  پلن مکتوب و تأیید صریح من. خواندن/بررسی/گزارش آزاد است.
- **قانون Graph API (تصحیح‌شده):** استفاده نمی‌کنیم چون در ایران به‌عنوان سرویس‌دهنده در دسترس
  نیست؛ اگر سرویسی (مثل ChatPlace) آن را مهیا کند، استقبال می‌کنیم. اتوماسیون مرورگر (مثل
  Windsor) طبق ToS متا ممنوع است — هرگز پیشنهاد نده.
- هر پرامتی که تمام می‌شود، باید **گزارش مکتوب در اسناد پروژه** بگذارد. مستندسازی بخشی از
  تمام‌کردن کار است، نه کار اضافه.
- **قبل از کدنویسی سند طراحی بنویس و تأیید بگیر.**
- فقط روی شاخهٔ کاری جلسه کار کن — شاخه را از سیستم بگیر، نه از متن اسناد.
- اپ‌های فلاتر (ادمین/مشتری/پیک) مبنای کارند و **هر اپ باید نسخهٔ وب‌اپ (PWA) هم داشته
  باشد**؛ کار اپ‌ها طبق `DESIGN-APPS-ROADMAP.md` در ریپوی اپ‌ها انجام می‌شود، نه این مخزن.
- قواعد فنی مهم (جزئیات در `PROJECT-STATE.md` بخش ۶): SQL همیشه MySQL؛ هرگز `__()` قبل از
  `init`؛ شمارش جدول‌ها با `grep -c '$sql[] = "CREATE TABLE'` (هم‌اکنون ۶۹)؛ تلهٔ
  `wpdb::$field_types` (`post_id` → %d)؛ `wp_get_referer()` در حالت self-POST false؛
  `random_int` برای امینت؛ لجر به‌جای موجودی ذخیره‌شده؛ idempotency مالی؛ `Logger::info` با
  پارامتر دوم **رشته**؛ آداپتورهای درگاه بانکی به `AbstractIpgGateway` و `use IGBZ\Suite\Support\Http;`
  نیاز دارند (فراموشش TypeError می‌دهد). تست‌ها `igbz_test_reset_settings()` را صدا بزنند و
  کیس جدید در `$cases` ثبت شود؛ تأیید زنده روی سایت، نه فقط تست واحد.
- **چیزهایی که هنوز مانده** (برای کارهای بعدی): طراحی هستهٔ هوشواره (AI مرکزی) — طبق قرار،
  فقط با دستور جداگانهٔ من؛ اپ‌های فلاتر؛ کلیدهای واقعی سرویس‌ها؛ بازسازی فایل `.pot`؛
  سیاست انقضای پست VIP (سه سؤال در `PROJECT-STATE.md` بخش ۲).
