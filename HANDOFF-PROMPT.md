# پرامپت شروع چت جدید

متن زیر را عیناً در چت تازه کپی کنید.

---

سلام. این پروژه از قبل شروع شده، کد زیادی دارد و چند فاز تکمیل شده است. قبل از هر کاری این
سه مرحله را به‌ترتیب انجام بده و **مرحله‌به‌مرحله** جلو برو. همهٔ پاسخ‌هایت را **فارسی** بنویس.

## در یک نگاه

مخزن `paymanshafayan/IGBZ-WP`. دو چیز داخلش هست:

1. **محصول IGBZ** — پورت از nopCommerce به **وردپرس + ووکامرس**، یک افزونه با پنج ماژول
   قابل‌خاموش‌وروشن (`multitenant`, `instagram`, `hub`, `rest_api`, `fx`). دیتابیس v17 با
   **۷۰ جدول**. فازهای ۶ تا ۱۴ نقشهٔ راه و واسط پرداخت ارزی تمام شده‌اند.
2. **هوشا** (`hoosha/`) — ابزار عاملِ خودمان، نسخهٔ **۰.۹.۲**: هستهٔ عامل با ابزار واقعی،
   هاب پرووایدر با عیب‌یاب، رابط دوزبانه، اتصال گیت با مخزنِ قفل‌شوندهٔ هر گفتگو، سندباکس
   کانتینری. ۳۶۵ تست واحد + ۴۴ آزمون زنده. قرار است بعداً هستهٔ **هوشواره** از دلش دربیاید.

> شاخهٔ کاری این جلسه را **از سیستم بگیر** (`git status`)، نه از متن این سند. هر جلسه شاخهٔ
> `arena/…` خودش را دارد. `origin` منبع حقیقت است؛ اگر گیت محلی به‌هم ریخت:
> `git fetch origin && git reset --hard origin/<branch>`.

## مرحلهٔ ۱ — اسناد را بخوان

1. `PROJECT-STATE.md` — **اول این.** وضعیت کامل پروژه، تصمیم‌های ثبت‌شدهٔ کارفرما، و بخش
   **۷.۵** که هوشا را توضیح می‌دهد.
2. `MODULES-REFERENCE.md` — مرجع ماژول‌ها: سرویس‌ها، جداول، کرون‌ها، صفحات ادمین، REST،
   کلیدهای تنظیمات.
3. `igbz-suite/README.md` — مرجع فنی افزونه · `_devenv/AGENT-BRIEF.md` — جزئیات درونی
   زیرسیستم‌ها و تله‌هایی که قبلاً وقت زیادی گرفته‌اند.
4. `hoosha/README.md` — مرجع کامل هوشا · `hoosha/DESIGN-UI-PARITY.md` — تاریخچهٔ رابط و هر
   باگی که سر راه پیدا شد · `hoosha/DESIGN-PROVIDER-HUB.md` — هاب و عیب‌یاب.
5. سندهای طراحی فازها: `DESIGN-PHASE6-PAYMENTS.md`، `DESIGN-PHASE7-COURIER.md`،
   `DESIGN-PHASE9-MARKETPLACES.md`، `DESIGN-PHASE10-SEO.md`،
   `DESIGN-PHASE12-INTERNATIONAL.md`، `DESIGN-DOMAIN.md`، `DESIGN-LEGAL-AUTH.md`،
   `DESIGN-FX.md`، `DESIGN-APPS-ROADMAP.md`، `DESIGN-DEPLOY-HOOSHA.md`، `API-KEYS.md`،
   `PROMPTS-SEO-HUSHVARE.md`، `PROMPT-IG-GROWTH-HUSHVARE.md`.

**قانون اینستاگرام (تصحیح‌شده — مهم):** عدم استفاده از Graph API **تنها به‌دلیل نبودِ امکان
دریافت آن به‌عنوان سرویس‌دهنده در ایران است** — «نتوانستن»، نه «نخواستن». اگر سرویسی آن را
مهیا کند، استقبال می‌کنیم. اتوماسیون مرورگر (مثل Windsor) طبق ToS متا ممنوع است — هرگز
پیشنهاد نده.

## مرحلهٔ ۲ — محیط را بالا بیاور

**افزونه** (محیط توسعهٔ آفلاین داخل خود مخزن است، چون سندباکس به wordpress.org دسترسی ندارد):

```bash
ls _devenv/.work/            # اگر خالی/نبود:
bash _devenv/setup.sh        # ~۲۳ ثانیه
bash _devenv/run.sh          # http://127.0.0.1:9400 با ورود خودکار مدیر
bash _devenv/test.sh         # باید بدهد: ۱۲۰۹ اظهارنظر / ۲۳ کیس، ۲۳۵ فایل بدون خطا
bash _devenv/makepot.sh --check   # قالب ترجمه کهنه است یا نه (بدون نوشتن)
```

**هوشا:**

```bash
cd hoosha && npm ci
node test/run.mjs            # ۳۶۵ تست واحد (~۳۰ ثانیه)
node test/live.mjs           # ۴۴ آزمون زنده روی یک سرور واقعی
node src/cli.js --version    # نسخه + شناسهٔ کامیت + مسیر واقعی کد
```

نکته‌هایی که وقتت را می‌گیرند اگر ندانی:

- بالا آمدن سایت ۳۰ تا ۹۰ ثانیه طول می‌کشد؛ یک ۵۰۲ پیش از اولین ۲۰۰ طبیعی است.
- روی نصب تازه، ووکامرس پنل مدیر را با ویزارد می‌دزدد؛ یک بار این را باز کن:
  `admin.php?page=wc-admin&path=%2Fsetup-wizard&step=skip-guided-setup`
- `_devenv/.work/` و `hoosha/node_modules/` بین جلسه‌ها پاک می‌شوند؛ هیچ کدِ پروژه‌ای آنجا
  نیست. سلامت پایگاه: `/?igbz_health=1` باید ۷۰/۷۰ جدول بدهد.
- لاگ خطای وردپرس: جدیدترین
  `/tmp/node-playground-cli-site-*/wordpress/wp-content/debug.log`.
- هوشا را از **ریشهٔ مخزن** با `.\hoosha.cmd` یا `./hoosha.sh` اجرا کن؛ `node src/cli.js`
  از ریشه یعنی `MODULE_NOT_FOUND` با `requireStack: []`.
- **سندباکس هوشا پیش‌فرض روشن است.** روی ماشین بدون داکر، ابزار `bash` عمداً اجرا نمی‌شود.
  اگر باید روی پروژهٔ واقعی کار کند، تنظیمات ← سندباکس را صریح خاموش کن.

بعد وارد پنل شو، منوی **IGBZ** را باز کن و صفحه‌ها را ببین تا با محصول آشنا شوی — به‌خصوص
**Payments** (با دکمه‌های Test)، **Master payment**، **Logistics**، **Domain**،
**VIP channel**، **Courses** و **FX payments**.

## مرحلهٔ ۳ — گزارش بده و بایست

وقتی اسناد را خواندی و محیط بالا آمد، گزارش بده:

- محیط بالا آمد یا نه، و هر دو سوئیت (افزونه و هوشا) چه عددی دادند.
- از خواندن اسناد چه فهمیدی — وضعیت پروژه را با زبان خودت خلاصه کن.
- اگر جایی از اسناد با کد نخواند، همان‌جا را بگو.
- کدام تصمیم‌ها منتظر رأی من است.

**بعد از گزارش، متوقف شو و منتظر بمان.** خودت کاری را شروع نکن، هیچ فایلی را عوض نکن، و
پیشنهاد اجرا نده مگر بخواهم. بعد از گزارش می‌گویم چه کاری انجام شود.

## قاعده‌های ثابت

- **فارسی** بنویس.
- **بدون تغییر بدون پلن تأییدشده:** هیچ تغییری در کد، سند، کامیت یا پوش مگر بعد از پلن
  مکتوب و تأیید صریح من. خواندن و بررسی و گزارش آزاد است.
- **تأیید زنده، نه فقط تست واحد.** بعد از هر تغییر معنادار، روی سایت (یا رابط هوشا) کلیک
  کن. چند باگ فقط این‌طور پیدا شدند: `wp_get_referer()` در self-POST، `TypeError` لاگر که
  کل مسیر بازگشت وجه را خواباند، و وارونگی اولویت قیف.
- **سوئیتی که بار اول سبز شود چیزی ثابت نکرده.** هر تست تازه را با خراب‌کردن عمدی کد قرمز
  کن، بعد درستش کن.
- **تست‌ها را ته کار بگیر، نه بعد از هر ویرایش.** حین کار:
  `node test/run.mjs --only <الگو>`؛ پیش از کامیت، اجرای کامل.
- هر کاری که تمام می‌شود باید **گزارش مکتوب در اسناد** بگذارد؛ مستندسازی بخشی از
  تمام‌کردن کار است، نه کار اضافه.
- **قبل از کدنویسی سند طراحی بنویس و تأیید بگیر.**
- فقط روی شاخهٔ کاری جلسه کار کن — شاخه را از سیستم بگیر، نه از متن اسناد.
- اپ‌های فلاتر (ادمین/مشتری/پیک) مبنای کارند و هر اپ باید نسخهٔ **وب‌اپ (PWA)** هم داشته
  باشد؛ کارشان در ریپوی اپ‌ها انجام می‌شود، نه این مخزن.
- قواعد فنی افزونه (جزئیات در `PROJECT-STATE.md` §۶): SQL همیشه MySQL؛ هرگز `__()` قبل از
  `init`؛ شمارش جدول‌ها با `grep -c '$sql[] = "CREATE TABLE'` (اکنون ۷۰)؛ تلهٔ
  `wpdb::$field_types` (`post_id` → %d)؛ `wp_get_referer()` در self-POST مقدار false
  می‌دهد؛ `random_int` برای امنیت؛ لجر به‌جای موجودیِ ذخیره‌شده؛ idempotency مالی؛
  `Logger::info` با پارامتر دوم **رشته**؛ آداپتور درگاه بانکی به `AbstractIpgGateway` و
  `use IGBZ\Suite\Support\Http;` نیاز دارد (فراموشش TypeError می‌دهد). تست‌ها
  `igbz_test_reset_settings()` را صدا بزنند و کیس تازه در `$cases` ثبت شود.

## آنچه هنوز باز است (کارهای بعدی)

1. **طراحی هستهٔ هوشواره (AI مرکزی)** — طبق قرار، فقط با دستور جداگانهٔ من.
2. **اپ‌های فلاتر** (ادمین/مشتری/پیک + PWA) — طبق `DESIGN-APPS-ROADMAP.md`، در ریپوی اپ‌ها.
3. **کلیدهای واقعی** سرویس‌ها (بانک‌ها، چت‌پلیس/منی‌چت، باسلام، گوگل، شاهکار، کاوه‌نگار،
   Manus و…) — آداپتورها آماده‌اند؛ تست واقعی فقط با کلید واقعی.
4. **ترجمهٔ فارسی `fa_IR.po`** — قالب `.pot` به‌روز است (۲۳۰۸ رشته)؛ ترجمهٔ کامل یا فقط
   رشته‌های روبه‌مشتری، تصمیم من است.
5. **استقرار هوشا روی سرور** — `DESIGN-DEPLOY-HOOSHA.md` نوشته شده، **پیاده نشده**، و هفت
   سؤال باز دارد (وب‌سرور، مدیر ارشد، حافظهٔ سرور، جای عامل‌های فروشگاه، محیط‌ها، عدد
   بودجه، نگه‌داری دفتر رویداد).
6. **در خود هوشا:** عدد بودجهٔ ماهانه (مکانیزم هست، عدد را من می‌دهم) · Fusion (تصمیم ۹،
   عمداً کنار گذاشته شد) · elicitation در MCP · ترمینال داخل برنامه · ترجمهٔ محتوای سرور
   (فعلاً فقط رابط ترجمه می‌شود).
7. **محدودیت شناخته‌شده (نه باگ):** انتخابگر پست در فرم قیف فقط محتوایی را نشان می‌دهد که
   از راه همین افزونه منتشر شده باشد؛ بدون Graph API راهی برای شمارش پست‌های یک صفحه نیست.
