# سیاست انقضای پست VIP — طراحی

**وضعیت: تأییدشده و پیاده‌سازی‌شده** (۱۴۰۵/۰۵/۲۶، DB v17). هر چهار مورد بخش ۸ را کارفرما
تأیید کرد. گزارش اجرا در بخش ۹ همین سند و در `PROJECT-STATE.md` بخش ۴.
**مبنا:** تصمیم کارفرما (۱۴۰۵/۰۵/۲۶) که سه سؤال باز بخش ۲ `PROJECT-STATE.md` را می‌بندد.

---

## ۱. تصمیم کارفرما (نقل مستقیم)

> مدت اعتبار هر پست در پنل مدیر ارشد IGBZ ثبت می‌شود و مدت پیش‌فرض یک هفته است و پس از آن پست
> از روی سرور حذف می‌شود. برای ادمین فروشگاه این مدت در بخش VIP پنل مدیریت فروشگاه اطلاع‌رسانی
> شود. تذکر داده شود با ارسال پست به کلوز فرند پیج اینستا می‌تواند از پست نگهداری کند. برای
> مشتری نیز در صفحه دریافت اشتراک یا خرید پست، اطلاع‌رسانی شود. و ذکر شود که مشتری می‌تواند با
> استفاده از آیکون ذخیره پست آن را در اپ مشتری ذخیره کند.

پس سه سؤال باز این‌طور بسته می‌شوند:

| سؤال باز (PROJECT-STATE §۲) | پاسخ |
|---|---|
| مدت پیش‌فرض چند روز؟ | **۷ روز**، تنظیم‌شونده فقط توسط **مدیر ارشد IGBZ** |
| پنهان یا حذف واقعی؟ | **حذف از سرور** |
| خریدار تک‌پست بعد از انقضا دسترسی دارد؟ | **نه** — ولی **قبل از پرداخت** به او اطلاع داده می‌شود و راه نگهداری (ذخیره در اپ) به او گفته می‌شود |

---

## ۲. وضعیت فعلی کد (قبل از هر تغییری)

| چیز | حالا | باید بشود |
|---|---|---|
| `vip.default_expiry_days` | `0` (بدون انقضا) | **`7`** |
| `vip.default_expiry_action` | `hide` | **`delete`** |
| `vip.purge_media_on_expiry` | `true` ✅ | بدون تغییر |
| جای تنظیم | IGBZ → Settings → VIP (فقط مدیر ارشد، `igbz_manage_suite`) ✅ | بدون تغییر — همان «پنل مدیر ارشد» است |
| ویرایشگر پست (پنل فروشگاه) | ادمین فروشگاه می‌تواند `expires_at` و `expiry_action` **هر پست** را آزادانه عوض کند | باید فقط **اطلاع‌رسانی** باشد، نه کنترل (بخش ۴) |
| اطلاع‌رسانی به ادمین فروشگاه | ندارد | بنر در صفحهٔ VIP + تذکر کلوز فرند |
| اطلاع‌رسانی به مشتری | صفحهٔ فرود فقط یک برچسب کوچک «تا فلان تاریخ» دارد | جملهٔ صریح کنار دکمه‌های خرید |
| موتور انقضا (`expire_due`) | ساخته شده، کرون پنج‌دقیقه‌ای، مدیا را پاک می‌کند ✅ | بدون تغییر منطقی |

### ⚠️ یافتهٔ مهم: «آیکون ذخیره» **اصلاً وجود ندارد**

اسناد پروژه (`DESIGN-VIP.md` §۳، `igbz-suite/README.md`، `PROJECT-STATE.md` §۲،
`AGENT-BRIEF` §۷) هر چهار جا نوشته‌اند لایهٔ اجتماعی VIP شامل **«ذخیره / بوکمارک»** است.
**در کد نیست:**

- در `VipSocialService` فقط `toggle_like`، `add_comment`، `comments`، `set_comment_status`،
  `pin_comment` و `record_view` وجود دارد — هیچ متد ذخیره‌ای نیست.
- جدولی به‌نام `vip_post_saves` در `Schema.php` نیست (نه جدول `vip_*` داریم، هیچ‌کدام ذخیره نیست).
- در ۲۹ مسیر `/vip/*` هیچ مسیر save/bookmark ثبت نشده.

یعنی بندی از تصمیم شما به قابلیتی ارجاع می‌دهد که ساخته نشده. باید ساخته شود — و **مهم‌تر
از ساختنش، معنایش است** (بخش ۳).

---

## ۳. تنها ابهام واقعی: «ذخیره» یعنی چه؟

اگر پست از سرور حذف شود، **بوکمارک سمت سرور بی‌معنا است**: یک اشاره‌گر به چیزی که دیگر نیست.
پس «ذخیره» فقط وقتی وعدهٔ صادقانه‌ای است که یعنی **نسخهٔ آفلاین روی دستگاه خود مشتری**.

| گزینه | یعنی | بعد از حذف پست |
|---|---|---|
| الف) بوکمارک سمت سرور | ردیفی در دیتابیس ما | ❌ خالی می‌شود — به مشتری دروغ گفته‌ایم |
| **ب) نسخهٔ آفلاین در اپ** (پیشنهاد من) | اپ فایل مدیا + کپشن را داخل حافظهٔ خصوصی خودش کپی می‌کند | ✅ مشتری همچنان دارد |

**پیشنهاد: گزینهٔ ب.** ولی این یک تصمیم قبلی را نقض می‌کند و باید صریح تأییدش کنید:

> `DESIGN-VIP.md` §۲ نوشته: «دکمهٔ اشتراک‌گذاری و دانلود در اپ وجود ندارد».

پیشنهاد من برای آشتی‌دادن این دو: **ذخیرهٔ داخل‌اپی، نه دانلود به گالری.** فایل در
sandbox خصوصی اپ می‌نشیند، در گالری/فایل‌منیجر گوشی ظاهر نمی‌شود، دکمهٔ اشتراک‌گذاری هم ندارد.
این با سطح امنیت VIP (سبک، هم‌تراز کلوز فرند) سازگار است — کسی که بخواهد، همین حالا هم
اسکرین‌شات می‌گیرد. **این آزادی هرگز به LMS تسری پیدا نمی‌کند** (آنجا امنیت سنگین است و
دانلود ممنوع).

---

## ۴. اختیار ادمین فروشگاه روی مدت

شما گفتید مدت را **مدیر ارشد** ثبت می‌کند و ادمین فروشگاه فقط **مطلع** می‌شود. الان ادمین
فروشگاه می‌تواند برای هر پست تاریخ دلخواه بگذارد و حتی «حذف» را به «پنهان» تغییر دهد.

**پیشنهاد:** فیلدهای `expires_at` و `expiry_action` از ویرایشگر پست **برداشته شوند** و جایشان
یک خط اطلاع‌رسانی بنشیند:

> «این پست ۷ روز پس از انتشار از سرور حذف می‌شود. این مدت را مدیر IGBZ تعیین می‌کند.»

ستون‌های دیتابیس و منطق سرویس دست‌نخورده می‌مانند (مدیر ارشد و مهاجرت‌ها از آن‌ها استفاده
می‌کنند)؛ فقط کنترلِ ادمین فروشگاه برداشته می‌شود. اگر ترجیح می‌دهید ادمین فروشگاه بتواند مدت
را **کوتاه‌تر** کند (نه بلندتر)، بگویید تا سقف‌دار پیاده شود.

---

## ۵. «حذف از سرور» دقیقاً یعنی چه

سه چیز از هم جدا هستند و همه را نباید یک‌جا پاک کرد:

| چیز | سرنوشت | چرا |
|---|---|---|
| **فایل مدیا** (عکس/ویدیو) | **واقعاً پاک می‌شود** (`purge()`، فقط داخل پوشهٔ uploads) | همین چیزی است که دیسک می‌خورد و همان چیزی که شما گفتید |
| **ردیف پست** | می‌ماند با وضعیت `deleted` (نامرئی برای همه: فید، صفحهٔ فرود، مدیا، اپ) | آمار فروش و کامنت‌ها به آن گره خورده‌اند |
| **رکوردهای مالی** (`vip_entitlements`، پرداخت‌ها) | دست‌نخورده | حذفشان یعنی پاک‌کردن سند خرید مشتری — نه از نظر حسابداری درست است نه از نظر قانونی |

پس «از روی سرور حذف می‌شود» = **محتوا واقعاً می‌رود**؛ آنچه می‌ماند فقط سند و آمار است، نه
قابل‌دیدن برای مشتری. اگر منظورتان پاک‌کردن کامل ردیف هم بود بگویید، ولی توصیه نمی‌کنم:
گزارش درآمد کانال VIP بعد از هر هفته صفر می‌شود.

---

## ۶. متن‌های اطلاع‌رسانی (پیشنهادی — هر کلمه‌اش قابل تغییر)

### ۶.۱ ادمین فروشگاه — بنر بالای صفحهٔ VIP و کنار فرم ساخت پست

> **پست‌های VIP ماندگار نیستند.** هر پست ۷ روز پس از انتشار از سرور حذف می‌شود و فایلش
> برگشت‌پذیر نیست. این مدت را مدیر IGBZ تعیین می‌کند.
> اگر می‌خواهید محتوا برایتان بماند، **قبل از انقضا همان پست را در کلوز فرندز پیج اینستاگرام
> خودتان هم بگذارید**؛ آنجا برای خودتان باقی می‌ماند.

### ۶.۲ مشتری — صفحهٔ فرود `/vip/p/{shortcode}`، بالای دکمه‌های «خرید اشتراک» و «خرید همین پست»

> این پست تا **{تاریخ}** در دسترس است و بعد از آن از سرور حذف می‌شود.
> می‌خواهید برایتان بماند؟ بعد از خرید، در اپ روی **آیکون ذخیره** بزنید تا نسخه‌اش داخل اپ
> خودتان بماند.

روی پست‌های آزاد/رایگان هم همین جمله (بدون بخش خرید) نشان داده می‌شود.

### ۶.۳ اپ مشتری — REST

فیلدهای `expires_at`، `retention_days` و یک `expiry_notice` آماده به فید و صفحهٔ پست اضافه
می‌شوند تا اپ فلاتر دقیقاً همین جمله را نشان دهد و در اسناد اپ دوباره نوشته نشود.

---

## ۷. کاری که سمت سرور انجام می‌شود

| # | کار | فایل |
|---|---|---|
| ۱ | پیش‌فرض‌ها: `vip.default_expiry_days = 7`، `vip.default_expiry_action = delete` | `Activator::seed_defaults()` |
| ۲ | مهاجرت **v17**: روی نصب‌های موجود همان دو کلید اصلاح شود و برای پست‌های منتشرشدهٔ بدون تاریخ، `expires_at = published_at + ۷ روز` نوشته شود | `Activator::migrate_to_v17()` |
| ۳ | متن کمکی تنظیمات VIP بازنویسی شود (الان «پنهان‌کردن امن‌تر است» را توصیه می‌کند — برخلاف تصمیم شما) | `SettingsPage.php` |
| ۴ | برداشتن کنترل انقضا از ویرایشگر پست + بنر اطلاع‌رسانی + تذکر کلوز فرند | `Admin/VipPage.php` |
| ۵ | جملهٔ اطلاع‌رسانی مشتری کنار دکمه‌های خرید | `Vip/VipLandingPage.php` |
| ۶ | **قابلیت ذخیره:** جدول `vip_post_saves` (v17)، متدهای `toggle_save`/`saved_posts`، مسیرهای `POST /vip/posts/{id}/save` و `GET /vip/saved` | `Schema.php`، `VipSocialService`، `VipController` |
| ۷ | **اندپوینت نسخهٔ آفلاین:** `GET /vip/posts/{id}/offline` — فقط برای کاربر مستحق و فقط تا قبل از انقضا، بایت‌های مدیا را برای کپی داخل اپ می‌دهد | `VipController` + `VipMediaService` |
| ۸ | `expires_at` / `retention_days` / `expiry_notice` در پاسخ فید و پست | `VipPostService::present()` |
| ۹ | تست‌ها: پیش‌فرض ۷ روزه، حذف واقعی مدیا، نامرئی‌شدن پست برای خریدار تکی، ذخیره/برداشتن ذخیره، رد شدن `offline` برای غیرمستحق و برای پست منقضی | `tests/VipChannelTest.php` (کیس موجود) |
| ۱۰ | بازسازی `.pot` و به‌روزرسانی `PROJECT-STATE.md` / `DESIGN-VIP.md` / `README` | — |

**سمت اپ فلاتر (در ریپوی اپ‌ها، نه اینجا):** آیکون ذخیره، دانلود نسخهٔ آفلاین در sandbox اپ،
و نمایش «۳ روز باقی مانده» روی پست. در `DESIGN-APPS-ROADMAP.md` ثبت می‌شود.

**بدون تماس با Graph API.** تذکر کلوز فرند فقط **یک جملهٔ راهنما به یک انسان** است؛ هیچ
اتوماسیون و هیچ فراخوانی API اینستاگرامی در کار نیست.

---

## ۸. چیزهایی که باید تأیید کنید

1. **«ذخیره» = نسخهٔ آفلاین داخل اپ** (بخش ۳، گزینهٔ ب) — و پذیرش اینکه این، قید «بدون دانلود»
   در `DESIGN-VIP.md` §۲ را برای VIP نرم می‌کند (LMS دست‌نخورده می‌ماند).
2. **برداشتن اختیار انقضا از ادمین فروشگاه** (بخش ۴) — یا نسخهٔ سقف‌دار «فقط کوتاه‌تر».
3. **ماندن ردیف پست و رکوردهای مالی** بعد از پاک‌شدن فایل (بخش ۵).
4. **متن‌های سه‌گانهٔ اطلاع‌رسانی** (بخش ۶) — هر کلمه‌ای را می‌خواهید عوض کنید، همین‌جا بگویید
   تا در کد بنشیند.

با «تأیید» شما، بخش ۷ پیاده می‌شود، روی سایت زنده تأیید می‌گیرد و در PR شمارهٔ ۳ اضافه می‌شود.

---

## ۹. گزارش اجرا (۱۴۰۵/۰۵/۲۶ — تمام)

کارفرما هر چهار مورد بخش ۸ را تأیید کرد و همان روز پیاده شد. **نسخهٔ دیتابیس ۱۷، مجموع ۷۰ جدول.**

| # | کار | نتیجه |
|---|---|---|
| ۱ | پیش‌فرض‌ها: `vip.default_expiry_days = 7`، `vip.default_expiry_action = delete`، کلید جدید `vip.offline_link_ttl = 3600` | `Activator::seed_defaults()` |
| ۲ | مهاجرت **v17** | روز صفر (یا کمتر) به ۷ می‌رود، ولی عدد مثبتی که مدیر ارشد خودش انتخاب کرده دست‌نخورده می‌ماند؛ `hide` به `delete` تبدیل می‌شود؛ پست‌های منتشرشدهٔ بدون تاریخ، `published_at + ۷ روز` می‌گیرند (خواندن + آپدیت سطر‌به‌سطر، چون UPDATE همبسته روی SQLite می‌شکند) |
| ۳ | متن‌های تنظیمات VIP | «پنهان‌کردن امن‌تر است» حذف شد؛ حالا سیاست مصوب و پیامدش توضیح داده می‌شود |
| ۴ | ویرایشگر پست فروشگاه | فیلدهای `expires_at` و `expiry_action` **حذف** شدند (از `$_POST` هم خوانده نمی‌شوند)؛ جایشان سطر فقط‌خواندنی «Expires» + بنر بالای صفحه با تذکر کلوز فرندز |
| ۵ | صفحهٔ فرود مشتری | `expiry_warning()` بالای دکمه‌های خرید، با تاریخ دقیق به وقت فروشگاه و دو نسخه (قبل از خرید / برای کسی که دارد) |
| ۶ | قابلیت ذخیره | جدول `vip_post_saves`، متدهای `toggle_save`/`has_saved`/`mark_offline`/`saved_post_ids`/`saved_count`، مسیرهای `POST /vip/posts/{id}/save` و `GET /vip/saved` |
| ۷ | نسخهٔ آفلاین | `GET /vip/posts/{id}/offline` — فقط مستحق، فقط تا قبل از انقضا، لینک امضاشدهٔ بلندتر (`vip.offline_link_ttl`)؛ دانلود خودش ذخیره را ثبت می‌کند (`offline_at`) |
| ۸ | خروجی اپ | `retention_days`، `expiry_notice` و `saved` به `present()` اضافه شد؛ یک جمله در یک جا، تا پنل و صفحهٔ فرود و اپ از هم جدا نیفتند |
| ۹ | تست | چهار سناریوی جدید در `VipChannelTest` → **۱۲۰۹ اظهارنظر در ۲۳ کیس**؛ ۲۳۵ فایل لینت |
| ۱۰ | `.pot` | بازسازی: ۲۳۱۸ رشته |

### چیزهایی که فقط تأیید زنده پیدا کرد

- **`TypeError` روی «پست جدید»:** `expiry_summary( array $post )` هنگام ساخت پست جدید `null`
  می‌گرفت و کل فرم را از «Publish at» به بعد می‌خواباند. امضا `?array` شد. هیچ تست واحدی
  نمی‌دیدش، چون فرم ادمین در هارنس رندر نمی‌شود.
- **یک متن متناقض:** پیشنهاد خرید تک‌پست می‌گفت «This post stays in your library» — که دقیقاً
  خلاف سیاست جدید است. بازنویسی شد: تا وقتی پست آنلاین است باز می‌شود، و بعدش نسخهٔ ذخیره‌شده.

### چیزی که در تست واحد پوشش داده نشد و چرا

`VipLandingPage::render()` با `exit` تمام می‌شود، پس فراخوانی‌اش از داخل تست، کل سوئیت را
می‌کشد. به‌جای اضافه‌کردن یک درز مصنوعی به کد محصول فقط برای تست، **متن هشدار** با Reflection
مستقیم تست می‌شود و **جایگاهش** (بالای دکمه‌ها) روی سایت زنده بررسی شد:
`igbz-vip-expiry-warning` قبل از اولین `igbz-vip-btn` رندر می‌شود.

### آنچه روی سایت زنده تأیید شد

۷۰/۷۰ جدول · بنر ۷ روزه در صفحهٔ VIP · ویرایشگر بدون کنترل انقضا و با سطر «Expires» ·
ساخت و انتشار پست واقعی (تاریخ حذف: ۷ روز بعد) · صفحهٔ فرود با هشدار بالای دکمهٔ «Buy this
post» · `save` → `{"saved":true}` · `offline` → لینک امضاشده + `notice` · `GET /vip/saved` ·
عقب‌بردن تاریخ و اجرای کرون → «۱ expired» → مدیا صفر، پست `deleted`، **ردیف ذخیره باقی**،
`offline` رد می‌شود، و صفحهٔ اشتراک **۴۱۰** می‌دهد.

### باقی‌مانده برای ریپوی اپ‌ها

آیکون ذخیره در فید، فراخوانی `offline` پس از ذخیره و نگهداری فایل در sandbox خصوصی اپ (نه
گالری، بدون دکمهٔ اشتراک‌گذاری)، و نمایش «۳ روز باقی مانده» روی پست — در
`DESIGN-APPS-ROADMAP.md` ثبت شد.
